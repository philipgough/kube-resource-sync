apiVersion: v1
kind: ConfigMap
metadata:
  name: sample-config
  namespace: default
data:
  config.yaml: |
    server:
      host: 0.0.0.0
      port: 8080
      timeout: 30s
    
    logging:
      level: info
      format: json
      
    features:
      metrics: true
      health_check: true
